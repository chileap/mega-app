(function(){
    // Remove any existing calendar form
  if (document.querySelector('#calendar-popover-container')) {
    document.querySelector('#calendar-popover-container').remove();
  }

  // Create a new calendar form
  const calendarDetail = "<%= j(render partial: 'timeboxing_items/timeboxing_item', layout: 'shared/calendars/popover_container', locals: { timeboxing_item: @timeboxing_item } ) %>";

  // Insert the calendar form into the DOM
  document.body.insertAdjacentHTML('beforeend', calendarDetail);
})()
