(function(){
  // Create a new calendar form
  const calendarDetail = "<%= j(render partial: 'tasks/task_details', layout: 'shared/calendars/popover_container', locals: { task: @task } ) %>";

  // Insert the calendar form into the DOM
  document.body.insertAdjacentHTML('beforeend', calendarDetail);
})()
